import 'package:flutter/material.dart';

class ElectricityPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Electricity Page'),
      ),
      body: const Center(
        child: Text('Electricity Content'),
      ),
    );
  }
}
