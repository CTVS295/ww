import 'package:flutter/material.dart';

class PSTNPaymentPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('PSTN Payment Page'),
      ),
      body: const Center(
        child: Text('PSTN Payment Content'),
      ),
    );
  }
}
