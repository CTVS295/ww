import 'package:flutter/material.dart';

class PhonePaymentPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Phone Payment Page'),
      ),
      body: const Center(
        child: Text('Phone Payment Content'),
      ),
    );
  }
}

