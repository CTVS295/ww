import 'package:flutter/material.dart';

class InternetPaymentPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Internet Payment Page'),
      ),
      body: const Center(
        child: Text('Internet Payment Content'),
      ),
    );
  }
}