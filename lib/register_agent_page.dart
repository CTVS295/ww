import 'package:flutter/material.dart';

class RegisterAgentPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Register Agent'),
      ),
      body: const Center(
        child: Text('Register Agent Page'),
      ),
    );
  }
}
