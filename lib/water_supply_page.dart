import 'package:flutter/material.dart';

class WaterSupplyPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Water Supply Page'),
      ),
      body: const Center(
        child: Text('Water Supply Content'),
      ),
    );
  }
}
