import 'package:flutter/material.dart';

class RegisterMerchantPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Register Merchant Page'),
      ),
      body: const Center(
        child: Text('Register Merchant Content'),
      ),
    );
  }
}
