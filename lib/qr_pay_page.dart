import 'package:flutter/material.dart';

class QRPayPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('QR Pay Page'),
      ),
      body: const Center(
        child: Text('QR Pay Content'),
      ),
    );
  }
}
